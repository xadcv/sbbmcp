{
  "framework": null,
  "buildCommand": "npm run build && mkdir -p public && echo '{}' > public/index.json",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/mcp", "destination": "/api/mcp" }],
  "functions": {
    "api/mcp.ts": {
      "maxDuration": 60
    }
  }
}
